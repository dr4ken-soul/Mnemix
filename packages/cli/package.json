{
  "name": "mnemix",
  "version": "0.1.0",
  "description": "Your terminal remembers every fix. You never debug the same error twice.",
  "type": "module",
  "bin": {
    "mnemix": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc && node scripts/copy-shell.mjs",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "supermemory": "latest",
    "commander": "^12.0.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
